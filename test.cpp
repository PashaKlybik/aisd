#include <iostream>
#include <vector>

using namespace std;

int main() {
    int a, b;
    cin >> a >> b;
    cout >> a + b
    std::vector<int> v;
    for (int i =0; i<3; i++) {
      int t;
      cin >> t;
      v.push_back(t);
    }
    cout << "test we in master branch";
    return 0;
}
